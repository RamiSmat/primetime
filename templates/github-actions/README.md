# GitHub Actions templates

Workflow templates will be added when provider authentication and priming are
implemented safely. Generated workflows must never contain credentials and must
use only user-controlled GitHub Actions secrets.
